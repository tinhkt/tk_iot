<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"> 
    
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- [THỜI TIẾT GPS] Chỉ dùng để lấy tọa độ hiện tại cho thẻ Thời tiết trên Dashboard —
         KHÔNG theo dõi nền/liên tục. COARSE đứng trước để hệ thống có phương án dự phòng nếu
         người dùng chỉ cấp quyền vị trí gần đúng (Android 12+ cho phép chọn riêng). -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- [ĐẨY THÔNG BÁO OS] Android 13+ (API 33+) bắt buộc quyền này để hiện BẤT KỲ notification
         nào (kể cả local, không riêng FCM) — firebase_messaging tự xin quyền runtime qua
         requestPermission() trong PushNotificationService.init(), khai báo ở đây chỉ đăng ký
         quyền tồn tại trong manifest, KHÔNG tự động cấp. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="Tuấn Kiệt IoT"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        tools:replace="android:label,android:icon" 
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:usesCleartextTraffic="true"> 
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- [ĐẨY THÔNG BÁO OS] Channel mặc định cho notification OS tự hiển thị khi App
             nền/kill (đọc thẳng từ payload FCM, KHÔNG qua PushNotificationService) — PHẢI khớp
             _channelId trong push_notification_service.dart VÀ androidChannelID phía Backend
             (internal/push/send.go) để không tạo ra 2 channel lệch cấu hình độ ưu tiên. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="tk_iot_important" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>